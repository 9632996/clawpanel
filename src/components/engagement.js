/**
 * Engagement hook reserved for the product-owned community entry.
 *
 * The upstream community QR codes and links are intentionally not wired here.
 * When AI作品 has an official community flow, implement it in this module.
 */
export function tryShowEngagement() {}

window.__testEngagement = function() {}
